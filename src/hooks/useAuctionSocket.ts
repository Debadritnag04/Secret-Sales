/**
 * @deprecated Socket management is now handled inside GameStateContext.
 * This file is kept for backwards compatibility but exports nothing.
 * Use `useGame()` from '../store/GameStateContext' for socket access.
 */
export {};
